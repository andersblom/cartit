import React, { Component } from 'react';

export default class UserSettings extends Component {
    render() {
        return(
            <div>
                settings
            </div>
        )
    }
}