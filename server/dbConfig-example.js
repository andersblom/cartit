'use strict';

// Change this file to dbConfig.js in this folder. 
// (end file path: ./server/dbConfig.js).

const dbConfig = {
    url: '<DB URL>', // mongodb:// is added in server.js
    port: 00000,
    user: '<DB USER>',
    password: '<DB PASSWORD>',
    name: '<DB NAME>'
}

module.exports = dbConfig;